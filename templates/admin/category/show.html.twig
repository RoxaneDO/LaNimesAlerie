{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block container %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ category.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_admin') }}">back to list</a>

    <a href="{{ path('category_edit', {'id': category.id}) }}">edit</a>

    {{ include('admin/category/_delete_form.html.twig') }}
{% endblock %}

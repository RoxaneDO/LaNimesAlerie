{% extends 'base.html.twig' %}

{% block container %}

<section class="container">
<div class="row row-cols-2">
    <div class="col">
        <div class="bg-danger d-flex justify-content-center align-items-center">
            <img src="{{ asset('image/promess-croquette.png') }}" class="imageProductPrincipal" alt="">
        </div>
        <div class="d-flex flex-content-between mt-3">
            <div class="bg-info w-50 me-3 d-flex justify-content-center align-items-center">
                <img src="{{ asset('image/promess-croquette.png') }}" class="imageProduct w-50" alt="">
            </div>
            <div class="bg-danger w-50 mx-3 d-flex justify-content-center align-items-center">
                <img src="{{ asset('image/promess-croquette.png') }}" class="imageProduct w-50" alt="">
            </div>
            <div class="bg-warning w-50 ms-3 d-flex justify-content-center align-items-center">
                <img src="{{ asset('image/promess-croquette.png') }}" class="imageProduct w-50" alt="">
            </div>
        </div>
    </div>
    <div class="col">
        <div class="">
            <h3 class="text-uppercase">{{ product.brand.name }}</h3>
            <h6>{{ product.name }}</h6>
            <p class="text-danger">à partir de <span class="fw-bolder text-danger">{{ product.priceHT }} EUR</span></p>
            <p><span>Régime alimentaire : </span> </p>
        </div>
        <div class="">
            <p>description</p>
        </div>

        <div class="">
            <label for="weight">Poids</label>
            <select class="" name="weight">
                <option value="1">1 kg</option>
                <option value="5">5 kg</option>
                <option value="10">10 kg</option>
                <option value="20">20 kg</option>
            </select>
        </div>


    </div>

</div>



    {{product.name}}
</section>


{% endblock %}
